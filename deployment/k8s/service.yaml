apiVersion: v1
kind: Service
metadata:
  name: panel-seg-api
  namespace: panel-seg
  labels:
    app.kubernetes.io/name: panel-seg
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: panel-seg
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 80
      targetPort: http